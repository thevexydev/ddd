module.exports = async (client) => {
  // Node events are now handled in client.js with Riffy
  // This file is kept for compatibility but events are managed by Riffy directly
  console.log("[NODE_HANDLER] Node events are now managed by Riffy in client.js");
}